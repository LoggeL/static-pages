{
  "name": "rectify-column-lab",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "prepare:cases": "node scripts/prepare-cases.mjs",
    "data": "uv run --with thermo==0.6.0 python scripts/generate-properties.py"
  }
}
